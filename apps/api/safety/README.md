# API Safety Module

Implementation lives in `apps/api/src/safety` and includes:

- Prompt policy classification
- Image screening hook
- Generation-time controls (negative prompt preset + parameter clamps)
- Policy rejection error handling
